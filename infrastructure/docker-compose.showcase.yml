services:
  web:
    build:
      context: ../frontend
      dockerfile: Dockerfile.showcase
    restart: unless-stopped
    ports:
      - "${PMGM_SHOWCASE_BIND_ADDRESS:-127.0.0.1}:${PMGM_SHOWCASE_HTTP_PORT:-8082}:80"
