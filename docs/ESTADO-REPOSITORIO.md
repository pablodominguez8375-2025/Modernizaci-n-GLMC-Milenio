# Estado del repositorio

A partir de la línea base `LB-PC-2026-09-17`, la documentación funcional vigente se mantiene en `docs/`.

El siguiente hito técnico es incorporar al repositorio el código funcional del backend, frontend, base de datos, infraestructura y pruebas, y desplegarlo de forma reproducible en `srv01` desde `/opt/centenario/app`.
